import React from 'react';
import './Header.css'; // Assuming we are using CSS for styling

const Header = () => {
    return (
        <header className="site-header">
            <h1>MIT Student House</h1>
        </header>
    );
};

export default Header;
